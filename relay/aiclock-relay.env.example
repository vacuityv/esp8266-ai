# Copy to /etc/aiclock-relay.env on the VPS and fill in real secrets.
# Generate with:  openssl rand -hex 16   /   openssl rand -hex 8
RELAY_PORT=8080
PUSH_TOKEN=replace-with-openssl-rand-hex-16
PULL_SECRET=replace-with-openssl-rand-hex-8
